{% block content %}
    <a href="{{ '/' | url}}">Home</a>
    <ul class="navigation">
        <li>
    Design System
    <ul>
                {% for item in design %}
                    <li>
                        <a href="{{ ('/design/' + item.title) | url }}">{{ item.title }}</a>
                    </li>
                {% endfor %}
            </ul>
        </li>
        <li>
    Components
    <ul>
                {% for item in components %}
                    <li>
                        <a href="{{ ('/components/' + item.title) | url }}">{{ item.title }}</a>
                    </li>
                {% endfor %}
            </ul>
        <li>
    </ul>
{% endblock %}
{% block content %}
<nav>
<ul class="navigation">
  <li>
    <p><a href="{{ '/' | url}}">Home</a></p>
    </li>
  <li>
    <ul class="navigation">
        <p>design system</p>
        {%  for item in designs %}
          <li>
          <p>
              <a href="{{ ('/designs/' + item.title) | url }}">{{ item.title }}</a>
              </p>
          </li>
        {% endfor %}
    </ul>
  </li>
  <li>
   <p>components</p>
    <ul class="navigation">
      {%  for item in components %}
      <li>
          <p><a href="{{ ('/components/' + item.title) | url }}">{{ item.title }}</a></p>
      </li>
      {% endfor %}
    </ul>
  <li>
    </ul>
    </nav>
{% endblock %}

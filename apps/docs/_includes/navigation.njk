{% block content %}
<nav>
  <vwc-sidenav-item href="{{ '/' | url}}" text="Home"></vwc-sidenav-item>
  <ul class="navigation">
    <p>design system</p>
    {%  for item in designs %}
      <li>
        <vwc-sidenav-item href="{{ ('/designs/' + item.title) | url }}" text="{{ item.title }}"></vwc-sidenav-item>
      </li>
    {% endfor %}
  </ul>
  <ul class="navigation">
    <p>components</p>
    {%  for item in components %}
      <li>
        <vwc-sidenav-item href="{{ ('/components/' + item.title) | url }}" text="{{ item.title }}"></vwc-sidenav-item>
      </li>
    {% endfor %}
  </ul>
</nav>
{% endblock %}

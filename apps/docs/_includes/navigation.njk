{% block content %}
<a href="{{ '/' | url}}">Home</a>
<ul class="navigation">
  <li>
    usage examples
    <ul>
      {%  for item in examples %}
      <li>
          <a href="{{ ('/examples/' + item.title) | url }}">{{ item.title }}</a>
      </li>
      {% endfor %}
    </ul>
  </li>
  <li>
    components
    <ul>
      {%  for item in components %}
      <li>
          <a href="{{ ('/components/' + item.title) | url }}">{{ item.title }}</a>
      </li>
      {% endfor %}
    </ul>
  <li>
</ul>
{% endblock %}

{% block content %}
<nav class="navigation">
  <p role="heading" aria-level="2">Design System</p>
  {%  for item in designs | sort(false, false, 'title') %}
  <a href="{{ ('/designs/' + item.title) | url }}" text="{{ item.title | replace("-", " ") | capitalize }}"></a>
  {% endfor %}
</nav>
<nav class="navigation">
  <p role="heading" aria-level="2">Components</p>
  {%  for item in components | sort(false, false, 'title') %}
  <a href="{{ ('/components/' + item.title) | url }}" text="{{ item.title | replace("-", " ") | capitalize }}"></a>
  {% endfor %}
</nav>
{% endblock %}

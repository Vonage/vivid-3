name: 🧬 Release

on:
  push:
    tags:
      - "deploy"
    branches:
      - 'main'

concurrency:
  group: ci-pre-release-${{ github.ref }}-1
  cancel-in-progress: true

jobs:
  call-deploy-site:
    needs: call-publish-package
    uses: vonage/vivid-3/.github/workflows/_deploy-deno.yml@main

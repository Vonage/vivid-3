name: '🧬 Pre Release'

on:
  push:
    branches: yinonov-build-action

concurrency:
  group: ci-pre-release-${{ github.ref }}-1
  cancel-in-progress: true

jobs:
  call-unit-tests:
    uses: vonage/vivid-3/.github/workflows/callables/unit-tests.yml@yinonov-build-action

  call-upload-artifact:
    needs: call-unit-tests
    uses: vonage/vivid-3/.github/workflows/callables/upload-artifact.yml@yinonov-build-action

  call-pre-release:
    needs: call-upload-artifact
    uses: vonage/vivid-3/.github/workflows/callables/publish-package.yml@yinonov-build-action

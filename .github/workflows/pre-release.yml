name: 🧬 Pre Release

on:
  push:
    branches: yinonov-build-action

concurrency:
  group: ci-pre-release-${{ github.ref }}-1
  cancel-in-progress: true

jobs:
  call-lint-and-build:
    uses: vonage/vivid-3/.github/workflows/_lint-and-build.yml@yinonov-build-action

  call-test-chrome:
    needs: call-lint-and-build
    uses: vonage/vivid-3/.github/workflows/_tests-all.yml@yinonov-build-action

  call-publish-package:
    needs: call-test-chrome
    uses: vonage/vivid-3/.github/workflows/_publish-package.yml@yinonov-build-action
